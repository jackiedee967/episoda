import {ExpandLeftLight as Component} from 'components/ui-pages/base/expand-left-light';
import type {StoryObj, Meta} from '@storybook/react';

type Story = StoryObj<typeof Component>;

const meta: Meta<typeof Component> = {
  title: 'UI Pages/ExpandLeftLight',
  component: Component,
};

export const ExpandLeftLight: Story = {
  // ...
};

export default meta;
