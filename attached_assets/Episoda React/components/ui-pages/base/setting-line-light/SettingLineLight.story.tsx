import {SettingLineLight as Component} from 'components/ui-pages/base/setting-line-light';
import type {StoryObj, Meta} from '@storybook/react';

type Story = StoryObj<typeof Component>;

const meta: Meta<typeof Component> = {
  title: 'UI Pages/SettingLineLight',
  component: Component,
};

export const SettingLineLight: Story = {
  // ...
};

export default meta;
